功能：账号管理-账号列表

  场景：进入账号列表页
    假设 用户已登录
    当 进入账号管理页面
    那么 显示账号列表第1页

  场景：按平台筛选账号
    当 选择「小红书」平台
    那么 列表仅显示小红书平台账号

  场景：按账号状态筛选
    当 选择「在线」状态
    那么 列表仅显示在线账号

  场景：搜索账号
    当 输入关键词「测试」并回车
    那么 列表显示匹配「测试」的账号

  场景：搜索账号无结果
    当 输入不存在关键词「xxx」并回车
    那么 显示空状态「没有搜索到账号」

  场景：重置筛选条件
    假设 已设置平台+状态+关键词筛选
    当 点击「重置」
    那么 平台恢复「全部」
    而且 状态恢复「全部」
    而且 关键词清空
    而且 列表重新加载

  场景：批量停止在线账号
    假设 账号A、B状态为「在线」
    当 选中账号A、B
    而且 点击「批量停止」
    而且 确认停止
    那么 选中账号状态变为「已停止」

  场景：批量启动已停止账号
    假设 账号A状态为「已停止」
    当 选中账号A
    而且 点击「批量启动」
    而且 确认启动
    那么 账号A状态变为「在线」

  场景：批量删除账号
    假设 账号A状态为「已停止」
    当 选中账号A
    而且 点击「批量删除」
    而且 确认删除
    那么 账号A从列表移除

  场景：单账号停止
    假设 账号A状态为「在线」
    当 点击账号A的「停止」按钮
    而且 确认停止
    那么 账号A状态变为「已停止」

  场景：单账号启动
    假设 账号B状态为「已停止」
    当 点击账号B的「启动」按钮
    那么 账号B状态变为「在线」

  场景：查看账号操作日志
    当 点击账号A的「日志」按钮
    那么 跳转账号日志页面

  场景：已停止账号可删除
    假设 账号D状态为「已停止」
    当 点击账号D的「删除」按钮
    而且 确认删除
    那么 账号D从列表移除

  场景：在线账号不可删除
    假设 账号E状态为「在线」
    当 点击账号E的「删除」按钮
    那么 提示「请先停止账号后再删除」

  场景：失效账号可删除
    假设 账号F状态为「失效」
    当 点击账号F的「删除」按钮
    而且 确认删除
    那么 账号F从列表移除
