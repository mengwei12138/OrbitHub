功能：添加账号

  场景：成功添加账号流程
    假设 用户进入添加账号页面
    当 选择平台「抖音」
    而且 点击「下一步」
    那么 进入登录认证页，展示二维码
    当 轮询 GET /api/v1/accounts/login/{sessionId}/status
    那么 等待扫码成功
    当 扫码成功
    那么 停留在二维码页，并显示确认继续按钮
    当 点击「确认进入后续认证」
    那么 展示手机号验证码认证表单
    当 输入收到的验证码
    而且 点击「完成认证」
    那么 提示「账号添加成功」
    而且 跳转账号列表

  场景：手机号格式校验
    假设 用户进入添加账号页面
    当 选择登录地区「重庆」
    而且 输入手机号「123」
    而且 失去焦点（手机号输入框）
    那么 显示错误「手机号格式不正确」

  场景：获取验证码-倒计时中禁止重复获取
    假设 用户选择登录地区「重庆」并点击「获取验证码」成功
    那么 按钮禁用，显示倒计时
    当 倒计时未结束
    那么 不可再次点击获取验证码

  场景：验证码错误不允许下一步
    假设 用户选择登录地区「重庆」并获取验证码成功
    当 输入错误验证码「0000」
    而且 点击「下一步」
    而且 调用 POST /api/v1/accounts/login/verify-code/submit 返回失败
    那么 提示 {error.message}
    而且 不进入扫码页

  场景：二维码过期可刷新
    假设 用户在扫码页
    当 二维码过期
    那么 显示「二维码已过期」
    而且 点击「刷新二维码」
    而且 调用 POST /api/v1/accounts/login/qr-code/refresh
    那么 获取新二维码和有效期

  场景：取消添加返回列表
    假设 用户在添加账号页
    当 点击「取消」
    那么 跳转账号列表

  场景：扫码页返回上一步停止轮询
    假设 用户在扫码页，正在轮询 sessionId 状态
    当 点击「返回上一步」
    那么 停止轮询 sessionId 状态
    而且 清理 sessionId 和二维码数据
    而且 返回手机验证步骤

  场景：平台+手机号已存在
    假设 用户选择登录地区「重庆」并输入的账号已存在
    当 点击「获取验证码」
    而且 调用 POST /api/v1/accounts/login/verify-code 返回失败
    那么 提示 {error.message}

  场景：验证码过期需重新获取
    假设 用户选择登录地区「重庆」并获取验证码后等待超过60秒
    而且 调用 POST /api/v1/accounts/login/verify-code/submit 返回失败
    当 输入验证码并点击「下一步」
    那么 提示 {error.message}

  场景：服务异常
    假设 用户选择登录地区「重庆」并点击「获取验证码」
    而且 调用 POST /api/v1/accounts/login/verify-code 返回失败
    那么 提示 {error.message}

  场景：下一步失败不进入扫码页
    假设 用户选择登录地区「重庆」并输入验证码
    当 点击「下一步」
    而且 调用 POST /api/v1/accounts/login/verify-code/submit 返回失败
    那么 提示 {error.message}
    而且 不进入扫码页

  场景：地区必填-未选地区禁止获取验证码
    假设 用户未选择登录地区
    当 选择平台「小红书」
    而且 输入手机号「13812345678」
    而且 点击「获取验证码」
    那么 提示「请选择登录地区」
    而且 阻止获取验证码

  场景：地区级联选择-先省后辖市
    假设 用户进入添加账号页面
    当 点击登录地区下拉
    而且 选择省份「广东」
    那么 显示城市列表
    当 选择城市「深圳市」
    那么 显示「深圳市」

  场景：地区级联选择-直辖市直接确定
    假设 用户进入添加账号页面
    当 点击登录地区下拉
    而且 选择直辖市「重庆」
    那么 直接显示「重庆」，无需再选城市
