功能：账号管理-添加账号

  场景：成功添加账号流程
    假设 用户进入添加账号页面
    当 选择平台「小红书」
    而且 输入手机号「13800138000」
    而且 点击「获取验证码」
    那么 按钮显示倒计时「60s」禁用

  场景：手机号格式校验-错误格式
    假设 用户进入添加账号页面
    当 输入手机号「123」
    而且 失去焦点
    那么 显示错误「手机号格式不正确」

  场景：手机号格式校验-正确格式
    假设 用户进入添加账号页面
    当 输入手机号「13812345678」
    那么 无格式错误提示

  场景：获取验证码-倒计时中禁止重复获取
    假设 用户点击「获取验证码」成功
    当 倒计时未结束
    那么 不可再次点击获取验证码

  场景：验证码错误不允许下一步
    假设 用户获取验证码成功
    当 输入错误验证码「0000」
    而且 点击「下一步」
    那么 提示验证码错误
    而且 不进入扫码页

  场景：二维码过期可刷新
    假设 用户在扫码页
    当 二维码过期
    那么 显示「二维码已过期」
    当 点击「刷新二维码」
    那么 获取新二维码和有效期

  场景：取消添加返回列表
    假设 用户在添加账号页
    当 点击「取消」
    那么 跳转账号列表

  场景：扫码页返回上一步停止轮询
    假设 用户在扫码页，正在轮询sessionId状态
    当 点击「返回上一步」
    那么 停止轮询sessionId状态
    而且 返回手机验证步骤

  场景：扫码成功后需手动确认进入后续认证
    假设 用户已进入登录认证页
    当 扫码成功且接口返回后续认证信息
    那么 页面停留在二维码区域
    而且 显示「确认进入后续认证」
    当 点击「确认进入后续认证」
    那么 展示验证码输入表单

  场景：平台+手机号已存在
    假设 用户输入的账号已存在
    当 点击「获取验证码」
    那么 提示「该平台下已存在该手机号」

  场景：验证码过期需重新获取
    假设 用户获取验证码后等待超过60秒
    当 输入验证码并点击「下一步」
    那么 提示「验证码已过期，请重新获取」

  场景：服务异常
    假设 用户点击「获取验证码」
    当 调用POST /api/v1/accounts/login/verify-code返回失败
    那么 提示「服务异常，请稍后重试」

  场景：下一步失败不进入扫码页
    假设 用户输入验证码
    当 点击「下一步」但爬虫返回失败
    那么 提示错误信息
    而且 不进入扫码页

  场景：添加账号-步骤指示器
    假设 用户在添加账号Step1页
    当 观察步骤指示器
    那么 显示「1 Phone Verification → 2 Scan Code Login」

  场景：上一步返回保留已填信息
    假设 用户在扫码登录页(Step2)
    当 点击「返回上一步」
    那么 返回Step1
    而且 保留已填写的手机号和验证码

  场景：Step1未填写禁用下一步
    假设 用户在添加账号表单
    当 未填写手机号或验证码
    那么 「下一步」按钮禁用

  场景：二维码刷新后倒计时重置
    假设 二维码展示中
    当 点击「刷新二维码」
    那么 倒计时重置为初始值

  场景：二维码超时后需刷新
    假设 二维码已超时
    当 等待倒计时结束
    那么 提示二维码已过期
    而且 需刷新获取新二维码

  场景：地区选择-默认选中
    假设 用户进入添加账号页面
    当 观察表单字段
    那么 默认选中配置中第一个地区（如「重庆」）

  场景：地区级联选择-先省后市
    假设 添加账号页，地区选择器已展示
    当 点击「登录地区」下拉
    而且 选择「广东」省份
    而且 在城市列表中选择「深圳市」
    那么 最终显示「深圳市」

  场景：地区必填-未选地区禁止获取验证码
    假设 添加账号页，清空地区选择
    当 输入手机号「13812345678」
    而且 点击「获取验证码」
    那么 提示「请选择登录地区」
    而且 阻止获取验证码
