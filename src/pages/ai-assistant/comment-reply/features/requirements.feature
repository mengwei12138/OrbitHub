功能：评论AI自动回复

  场景：进入评论回复页
    假设 用户已登录
    当 进入评论回复页面
    那么 显示今日数据看板
    而且 显示账号选择区域
    而且 显示回复规则设置区域

  场景：页面初始化显示骨架屏
    假设 页面正在加载
    当 数据加载中
    那么 显示骨架屏

  场景：今日数据看板展示
    假设 今日有评论数据
    那么 显示自动回复数量
    而且 显示屏蔽违规数量
    而且 显示待人工处理数量

  场景：点击待人工处理数字切换Tab
    假设 显示今日数据看板
    当 点击待人工处理数字
    那么 自动切换到「待人工处理」Tab

  场景：账号选择仅展示在线账号
    假设 账号列表有在线和离线账号
    当 进入账号选择区域
    那么 仅展示「在线」状态账号
    而且 离线账号展示在不可用区域

  场景：账号选择卡片式多选
    假设 在账号选择区域
    当 点击账号卡片
    那么 选中高亮
    当 再次点击
    那么 取消选中

  场景：全选当前页账号
    假设 在账号选择区域
    当 点击「全选」
    那么 仅选中当前页的在线账号
    而且 不跨页选中

  场景：按平台筛选账号
    假设 在账号选择区域
    当 点击「抖音」
    那么 仅显示抖音平台账号
    当 点击「小红书」
    那么 仅显示小红书平台账号

  场景：搜索账号
    假设 在账号选择区域
    当 输入账号名称关键词
    那么 显示匹配的账号列表

  场景：账号列表分页
    假设 账号列表有多页
    当 切换页码
    那么 显示对应页数据
    而且 每页默认5条

  场景：自动回复开关开启
    假设 在回复规则设置区域
    当 点击自动回复开关「开启」
    那么 新抓取评论自动按规则回复

  场景：自动回复开关关闭
    假设 在回复规则设置区域
    当 点击自动回复开关「关闭」
    那么 暂停自动回复

  场景：回复规则设置显示4种类型
    假设 在回复规则设置区域
    那么 显示正向、负向、中性、提问4种评论类型模板
    而且 各类型显示对应回复模板

  场景：编辑回复模板
    假设 在回复规则设置区域
    当 点击某类型的「编辑」
    那么 弹出编辑模板弹窗
    当 修改模板内容并确认
    那么 保存新的回复模板

  场景：新增自定义回复规则
    假设 在回复规则设置区域
    当 点击「+ 添加回复规则」
    那么 弹出新增规则弹窗
    当 填写类型名称、关键词、回复模板
    那么 新规则添加到规则列表

  场景：删除自定义回复规则
    假设 在回复规则设置区域
    当 点击自定义规则的「删除」
    那么 该规则从列表中移除
    而且 显示成功提示

  场景：回复规则内部滚动
    假设 规则超过6条
    当 规则列表超出高度
    那么 内部滚动（max-height）
    而且 不使用分页

  场景：设置回复语气
    假设 在回复规则设置区域
    当 选择回复语气（亲切/正式/活泼）
    那么 影响AI生成回复的语气

  场景：提问类评论需人工审核默认勾选
    假设 在人工干预设置区域
    那么 「提问类评论需人工审核后发送」默认勾选

  场景：负向评论需人工审核默认勾选
    假设 在人工干预设置区域
    那么 「负向评论需人工审核后发送」默认勾选

  场景：关键词屏蔽设置
    假设 在关键词屏蔽区域
    当 点击「+ 添加」
    那么 添加新的屏蔽词
    当 开启「自动删除包含屏蔽词的评论」
    那么 命中屏蔽词的评论自动删除无需确认

  场景：点击立即抓取
    假设 在评论回复页面
    当 点击「立即抓取」按钮
    那么 触发评论抓取
    而且 按钮进入60秒CD倒计时

  场景：AI分类评论
    假设 有新评论
    当 AI识别评论类型
    那么 按优先级处理冲突（提问>负向>正向>中性）
    而且 记录置信度

  场景：置信度低于70%进入待人工处理
    假设 AI分类置信度<70%
    那么 强制进入待人工处理列表
    而且 UI展示分类为「待确认」

  场景：Tab切换到待回复评论
    假设 在评论回复页面
    当 点击「待回复评论」Tab
    那么 显示待回复评论列表
    而且 不跳转页面

  场景：Tab切换到待人工处理
    假设 在评论回复页面
    当 点击「待人工处理」Tab
    那么 显示待人工处理列表

  场景：Tab切换到回复记录
    假设 在评论回复页面
    当 点击「回复记录」Tab
    那么 显示回复记录列表

  场景：待回复评论列表展示
    假设 待回复评论列表有数据
    那么 显示账号、评论内容、类型、操作列
    而且 传统分页，每页10条

  场景：待回复评论无数据显示空状态
    假设 无待回复评论
    那么 显示空状态插画
    而且 显示"暂无待回复评论"文案

  场景：AI建议回复
    假设 在待回复评论列表
    当 显示某评论的AI建议回复
    那么 展示AI根据规则生成的建议回复内容

  场景：自动回复评论
    假设 评论符合自动回复条件
    当 点击「自动回复」
    那么 直接使用AI建议回复

  场景：手动回复评论
    假设 在待回复评论列表
    当 点击某评论的「手动回复」
    那么 弹出手动回复Modal弹窗
    而且 显示原评论内容
    而且 输入框预填AI建议回复
    当 修改回复内容并确认
    那么 通过平台集成方式发送回复

  场景：删除评论
    假设 在待回复评论列表
    当 点击某评论的「删除评论」
    那么 弹出二次确认弹窗
    当 确认删除
    那么 通过平台集成方式删除评论

  场景：待人工处理列表操作
    假设 在待人工处理Tab
    当 显示某评论的AI分类和置信度
    那么 用户可选择分类
    而且 用户可编辑回复内容
    当 点击「确认回复」
    那么 发送回复
    当 点击「删除评论」
    那么 删除评论
    当 点击「跳过」
    那么 跳过该评论

  场景：回复记录列表展示
    假设 回复记录列表有数据
    那么 显示时间、评论内容、回复内容、状态、操作列
    而且 传统分页，每页10条

  场景：回复记录筛选
    假设 在回复记录Tab
    当 选择账号筛选条件
    或者 选择回复方式（自动/人工）
    或者 选择状态筛选
    那么 列表显示筛选后的结果

  场景：查看回复记录详情
    假设 在回复记录列表
    当 点击某记录的「查看详情」
    那么 弹出详情弹窗
    而且 显示完整评论内容和回复内容

  场景：账号离线显示警告
    假设 有账号离线
    当 在自动回复开关旁
    那么 显示⚠️警告图标
    当 鼠标悬停⚠️
    那么 显示具体离线账号列表

  场景：Token失效跳转登录页
    假设 用户已登录
    当 收到Token失效响应（401）
    那么 显示"登录已过期，请重新登录"
    而且 跳转到登录页

  场景：网络错误显示重试
    假设 页面加载失败
    当 网络连接失败
    那么 显示错误提示
    而且 提供重试按钮

  场景：点击返回按钮
    假设 用户在评论回复页面
    当 点击「返回」按钮
    那么 返回AI助手主页
    而且 路由变为 /ai-assistant
