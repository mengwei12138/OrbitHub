功能：AI助手-标签库管理

  场景：进入标签库页
    假设 用户已登录
    当 进入标签库页面
    那么 显示标签列表
    而且 显示筛选条件区域

  场景：页面初始化显示骨架屏
    假设 页面正在加载
    当 数据加载中
    那么 显示骨架屏

  场景：标签列表展示
    假设 标签列表有数据
    那么 显示分类、标签名称、适用平台、使用次数、状态、最近使用、操作列

  场景：标签列表分页
    假设 标签列表有多页
    当 切换页码
    那么 显示对应页数据
    而且 每页默认10条

  场景：切换每页条数
    假设 在标签列表
    当 选择每页条数
    那么 列表刷新显示对应条数

  场景：按分类筛选标签
    假设 在筛选条件区域
    当 选择分类筛选条件
    那么 列表显示对应分类标签

  场景：按状态筛选标签
    假设 在筛选条件区域
    当 选择状态筛选条件（启用/停用）
    那么 列表显示对应状态标签

  场景：搜索标签
    假设 在筛选条件区域
    当 输入标签名称或分类关键词
    那么 显示匹配的标签列表

  场景：重置筛选条件
    假设 已设置筛选条件
    当 点击「重置」
    那么 筛选条件恢复默认
    而且 列表重新加载

  场景：无标签数据显示空状态
    假设 标签列表为空
    那么 显示空状态插画
    而且 显示"暂无标签"文案

  场景：点击新建标签按钮
    假设 在标签库页面
    当 点击「新建标签」按钮
    那么 弹出新建标签弹窗

  场景：新建标签选择分类
    假设 在新建标签弹窗
    当 选择分类（热门推荐/内容分类/情感标签）
    那么 保存分类选择
    当 选择「+ 自定义」
    那么 显示自定义分类输入框

  场景：新建自定义分类
    假设 选择「+ 自定义」
    当 输入新分类名称
    而且 确认
    那么 新分类添加到分类列表

  场景：设置适用平台
    假设 在新建标签弹窗
    当 选择适用平台（全部/仅抖音/仅小红书）
    那么 保存平台设置

  场景：输入标签名称
    假设 在新建标签弹窗
    当 输入标签名称
    那么 标签名称以#开头

  场景：新建标签平台限制提示
    假设 标签适用平台为抖音
    那么 提示"抖音最多5个标签"
    当 适用平台为小红书
    那么 提示"小红书最多10个标签"

  场景：标签格式校验
    假设 输入标签名称
    当 输入纯数字或特殊字符
    那么 提示格式错误
    而且 提示"标签以#开头，最多30字符"

  场景：确认新建标签
    假设 填写完标签信息
    当 点击「确认」
    那么 调用新建接口
    而且 标签列表刷新

  场景：取消新建标签
    假设 在新建标签弹窗
    当 点击「取消」
    那么 关闭弹窗
    而且 不保存数据

  场景：编辑标签
    假设 在标签列表
    当 点击某标签的「编辑」
    那么 弹出编辑标签弹窗
    当 修改标签信息并确认
    那么 调用更新接口
    而且 标签列表刷新

  场景：停用标签
    假设 在标签列表
    当 点击某标签的「停用」
    那么 标签状态变为「停用」
    而且 标签不在发布页推荐

  场景：启用标签
    假设 某标签状态为「停用」
    当 点击该标签的「启用」
    那么 标签状态变为「启用」
    而且 标签恢复显示在发布页推荐

  场景：标签使用次数显示
    假设 标签有使用记录
    那么 显示系统内统计的使用次数

  场景：标签最后使用时间显示
    假设 标签有使用记录
    那么 显示最后使用时间

  场景：跨平台标签上限
    假设 同时发布到抖音和小红书
    那么 标签上限取最小值5个
    而且 显示提示文字

  场景：Token失效跳转登录页
    假设 用户已登录
    当 收到Token失效响应（401）
    那么 显示"登录已过期，请重新登录"
    而且 跳转到登录页

  场景：网络错误显示重试
    假设 页面加载失败
    当 网络连接失败
    那么 显示错误提示
    而且 提供重试按钮

  场景：点击返回按钮
    假设 用户在标签库页面
    当 点击「返回」按钮
    那么 返回AI助手主页
    而且 路由变为 /ai-assistant
