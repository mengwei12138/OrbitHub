功能：标签库管理

  场景：标签列表默认展示
    假设 用户已登录系统
    当 访问标签库页面
    那么 列表展示分类、标签名称、适用平台、使用次数、状态、最后使用、操作

  场景：标签列表分页
    假设 存在大于10条标签
    当 观察分页信息
    那么 传统分页，每页10条

  场景：标签列表分页-切换条数
    假设 存在大于10条标签
    当 选择每页20条
    那么 每页显示20条

  场景：标签列表-按分类筛选
    假设 存在多个分类标签
    当 选择分类「热门推荐」
    那么 显示该分类下的所有标签，其他分类标签隐藏

  场景：标签列表-按状态筛选
    假设 存在启用和停用标签
    当 选择状态「停用」
    那么 显示所有停用状态的标签

  场景：标签搜索-按名称
    假设 存在多个标签
    当 在搜索框输入标签名称关键词并点击查询
    那么 显示匹配的标签名称的记录

  场景：标签搜索-按分类
    假设 存在多个标签
    当 在搜索框输入分类名称关键词并点击查询
    那么 显示该分类下的标签记录

  场景：标签搜索-无结果
    假设 搜索不存在的标签
    当 输入不存在的标签并点击查询
    那么 显示空状态

  场景：重置筛选条件
    假设 已设置筛选条件
    当 点击「重置」
    那么 筛选条件恢复默认，列表刷新显示全部

  场景：使用次数展示
    假设 存在已使用的标签
    当 在标签列表中找到已使用的标签
    那么 显示该标签在系统内的使用次数

  场景：新建标签弹窗打开
    假设 用户已登录系统
    当 点击「新建标签」按钮
    那么 弹出新建标签弹窗

  场景：新建标签-选择分类
    假设 新建标签弹窗已打开
    当 选择「热门推荐」
    那么 分类切换为热门推荐

  场景：新建标签-自定义分类-原地展开
    假设 标签列表页面
    当 找到标签列表末尾的「自定义分类」按钮并点击
    那么 按钮位置原地展开分类名称输入框

  场景：新建标签-自定义分类-回车确认
    假设 输入框已展开
    当 输入新分类名称并按回车键
    那么 新分类添加到列表中，输入框收起

  场景：新建标签-自定义分类-点击确认
    假设 输入框已展开
    当 输入新分类名称并点击确认按钮
    那么 新分类添加到列表中，输入框收起

  场景：新建标签-自定义分类-重名校验
    假设 已存在某分类名称
    当 输入已存在的分类名称并按回车或点击确认
    那么 提示「分类名称已存在」，不添加重复分类

  场景：新建标签-自定义分类-取消输入
    假设 输入框已展开
    当 按ESC键或点击其他区域
    那么 输入框收起，不添加分类

  场景：新建标签-标签名重名校验
    假设 已存在某标签
    当 输入已存在的标签名并点击确认
    那么 提示「标签名称已存在」，不保存重复标签

  场景：新建标签-按钮位于列表末尾
    假设 用户已登录系统
    当 访问标签库页面并找到标签列表底部
    那么 「新建标签」按钮位于标签列表最后一行末尾

  场景：新建标签-点击后原地展开
    假设 用户已登录系统
    当 点击「新建标签」按钮
    那么 输入框在按钮位置原地展开，位置不变

  场景：新建标签-适用平台-全部
    假设 新建标签弹窗已打开
    当 选择「全部」
    那么 设置成功，标签适用于所有平台

  场景：新建标签-适用平台-仅抖音
    假设 新建标签弹窗已打开
    当 选择「仅抖音」
    那么 设置成功，标签仅适用于抖音

  场景：新建标签-适用平台-仅小红书
    假设 新建标签弹窗已打开
    当 选择「仅小红书」
    那么 设置成功，标签仅适用于小红书

  场景：标签名称格式-以井号开头
    假设 新建标签弹窗已打开
    当 在标签名称输入框输入「测试」
    那么 自动补充井号开头，显示为「#测试」

  场景：标签名称-实时字符计数
    假设 新建标签弹窗已打开
    当 在标签名输入框输入文字
    那么 实时显示已输入字符数

  场景：标签名称-30字符限制-输入31字符
    假设 新建标签弹窗已打开
    当 输入31个字符的标签名
    那么 提示「单标签最多30字符」

  场景：标签名称-不支持纯数字
    假设 新建标签弹窗已打开
    当 输入「123」作为标签名
    那么 提示「不支持纯数字」

  场景：标签名称-支持字母数字混合
    假设 新建标签弹窗已打开
    当 输入「test123」作为标签名
    那么 标签正常显示，自动补充井号开头为「#test123」

  场景：标签名称-支持中文
    假设 新建标签弹窗已打开
    当 输入「测试」作为标签名
    那么 标签正常显示，自动补充井号开头为「#测试」

  场景：标签名称-支持下划线
    假设 新建标签弹窗已打开
    当 输入「test_tag」作为标签名
    那么 标签正常显示，自动补充井号开头为「#test_tag」

  场景：标签名称-支持中划线
    假设 新建标签弹窗已打开
    当 输入「test-tag」作为标签名
    那么 标签正常显示，自动补充井号开头为「#test-tag」

  场景：标签名称-禁止特殊符号井号
    假设 新建标签弹窗已打开
    当 输入包含井号的标签名
    那么 提示「禁止特殊符号」，不保存

  场景：标签名称-禁止特殊符号美元
    假设 新建标签弹窗已打开
    当 输入包含美元的标签名
    那么 提示「禁止特殊符号」，不保存

  场景：标签名称-禁止特殊符号星号
    假设 新建标签弹窗已打开
    当 输入包含星号的标签名
    那么 提示「禁止特殊符号」，不保存

  场景：标签名称-禁止特殊符号方括号
    假设 新建标签弹窗已打开
    当 输入包含方括号的标签名
    那么 提示「禁止特殊符号」，不保存

  场景：标签名称-允许中英文数字组合
    假设 新建标签弹窗已打开
    当 输入「测试Test123」作为标签名
    那么 标签正常显示，自动补充井号开头

  场景：新建标签-确认创建
    假设 已填写标签信息（分类、名称）
    当 点击确认按钮
    那么 标签创建成功，弹窗关闭，列表刷新显示新标签

  场景：新建标签-取消
    假设 已填写标签信息
    当 点击取消按钮
    那么 弹窗关闭，不创建标签，列表无变化

  场景：编辑标签-弹窗打开
    假设 存在标签
    当 点击某标签行的「编辑」按钮
    那么 弹出编辑标签弹窗，显示当前标签信息

  场景：编辑标签-保存修改
    假设 编辑弹窗已打开
    当 修改标签信息并点击确认
    那么 标签信息更新成功，列表刷新显示新内容

  场景：编辑标签-取消
    假设 编辑弹窗已打开
    当 点击取消按钮
    那么 弹窗关闭，不保存修改，列表无变化

  场景：停用标签
    假设 存在启用状态标签
    当 点击某启用状态标签的「停用」按钮
    那么 标签状态变为停用，操作按钮变为「启用」

  场景：启用标签
    假设 存在停用状态标签
    当 点击某停用状态标签的「启用」按钮
    那么 标签状态变为启用，操作按钮变为「停用」

  场景：停用后启用-恢复推荐
    假设 标签处于停用状态
    当 点击该标签的「启用」
    那么 标签恢复正常推荐状态，可在发布页的标签建议中显示

  场景：删除标签-弹窗确认
    假设 存在标签
    当 点击某标签行的「删除」按钮
    那么 弹出确认删除弹窗，显示「确定删除该标签？」

  场景：删除标签-确认后列表更新
    假设 存在标签
    当 点击确认删除
    那么 标签从列表中移除，弹窗关闭

  场景：删除标签-取消
    假设 删除确认弹窗已打开
    当 点击取消按钮
    那么 弹窗关闭，标签保留，列表无变化

  场景：删除标签-有关联内容
    假设 标签已被内容使用
    当 删除已被内容引用的标签
    那么 提示「该标签已被使用，删除后将同步更新相关内容」

  场景：编辑标签-重名校验
    假设 存在多个标签
    当 点击某标签的编辑并修改名称为已存在的标签名
    那么 提示「标签名称已存在」，不保存修改

  场景：页面初始化骨架屏
    假设 网络正常
    当 首次访问标签库页
    那么 展示整体骨架屏，包含标题区、卡片区、列表区等主要区块占位符

  场景：列表刷新局部Loading
    假设 列表加载中
    当 触发列表刷新
    那么 表格区域显示局部Loading动画，筛选条件区域不受影响

  场景：按钮提交Loading
    假设 按钮点击提交
    当 点击确认按钮
    那么 按钮进入Loading状态，同时禁用防止重复提交

  场景：无数据空状态
    假设 不存在数据
    当 访问无数据的列表页
    那么 展示插画和「暂无数据」文字，不展示空白表格

  场景：搜索无结果空状态
    假设 搜索条件无匹配
    当 输入不存在的搜索条件并点击搜索
    那么 展示插画和「未找到相关内容，试试调整筛选条件」

  场景：Toast成功提示
    假设 操作成功
    当 触发成功操作
    那么 显示成功提示，2-3秒后自动消失

  场景：Toast失败提示
    假设 操作失败
    当 触发失败操作
    那么 显示失败提示，2-3秒后自动消失

  场景：Toast警告提示
    假设 操作异常
    当 触发异常操作
    那么 显示警告提示，2-3秒后自动消失

  场景：Toast点击关闭
    假设 Toast正在显示
    当 点击Toast关闭按钮
    那么 Toast立即关闭

  场景：确认弹窗-确认操作
    假设 弹窗已打开
    当 点击确认按钮
    那么 执行确认操作，弹窗关闭

  场景：确认弹窗-取消操作
    假设 弹窗已打开
    当 点击取消按钮
    那么 取消操作，弹窗关闭

  场景：确认弹窗-点击遮罩关闭
    假设 弹窗已打开
    当 点击弹窗外部遮罩区域
    那么 弹窗关闭，等同于取消操作

  场景：确认弹窗-ESC关闭
    假设 弹窗已打开
    当 按下ESC键
    那么 弹窗关闭，等同于取消操作

  场景：网络断开-列表重试
    假设 网络断开
    当 访问列表页
    那么 列表区域显示加载失败提示和重试按钮

  场景：接口超时-列表重试
    假设 接口响应超时
    当 访问列表页
    那么 列表区域显示加载超时提示和重试按钮

  场景：Token失效-跳转登录
    假设 Token已过期
    当 触发需要Token的接口
    那么 跳转登录页，提示登录已过期
