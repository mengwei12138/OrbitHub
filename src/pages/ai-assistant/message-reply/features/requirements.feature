功能：私信AI自动回复

  场景：进入私信回复页
    假设 用户已登录
    当 进入私信回复页面
    那么 显示账号选择区域
    而且 显示抓取设置区域
    而且 显示分类与回复规则区域

  场景：页面初始化显示骨架屏
    假设 页面正在加载
    当 数据加载中
    那么 显示骨架屏

  场景：账号选择全选
    假设 在账号选择区域
    当 点击「全选」
    那么 选中所有在线账号

  场景：按平台筛选账号
    假设 在账号选择区域
    当 点击「抖音」
    那么 仅显示抖音平台账号
    当 点击「小红书」
    那么 仅显示小红书平台账号

  场景：账号卡片多选
    假设 在账号选择区域
    当 点击账号卡片
    那么 选中高亮
    当 再次点击
    那么 取消选中

  场景：设置抓取频率
    假设 在抓取设置区域
    当 选择抓取频率（5/15/30/60分钟）
    那么 保存抓取频率设置

  场景：设置私信类型筛选
    假设 在抓取设置区域
    当 选择私信类型（全部/未读/已读）
    那么 保存筛选条件

  场景：点击立即抓取
    假设 在私信回复页面
    当 点击「立即抓取」按钮
    那么 触发私信抓取
    而且 按钮进入60秒CD倒计时

  场景：自动回复开关开启
    假设 在分类与回复规则区域
    当 点击自动回复开关「开启」
    那么 新抓取私信自动按规则回复

  场景：自动回复开关关闭
    假设 在分类与回复规则区域
    当 点击自动回复开关「关闭」
    那么 暂停自动回复

  场景：分类规则显示4种分类
    假设 在分类与回复规则区域
    那么 显示合作咨询、投诉建议、产品咨询、垃圾信息4种分类
    而且 各分类显示关键词匹配和回复模板

  场景：新增分类规则
    假设 在分类与回复规则区域
    当 点击「+ 添加分类」
    那么 弹出新增分类弹窗
    当 填写分类名称、关键词、回复模板
    那么 新分类添加到规则列表

  场景：编辑分类规则
    假设 在分类与回复规则区域
    当 点击某分类的「编辑」
    那么 弹出编辑分类弹窗
    当 修改内容并确认
    那么 保存新的分类规则

  场景：分类规则内部滚动
    假设 分类超过6条
    当 分类列表超出高度
    那么 内部滚动（max-height）
    而且 不使用分页

  场景：设置重要私信标记
    假设 在重要私信标记区域
    当 勾选某分类为重要
    那么 该分类私信自动标记为重要

  场景：设置重要私信通知方式
    假设 在重要私信标记区域
    当 选择通知方式（站内通知/短信/邮件）
    那么 保存通知设置

  场景：待处理私信列表展示
    假设 待处理私信列表有数据
    那么 显示账号、发送者、私信内容、分类、操作列
    而且 传统分页，每页10条

  场景：按账号筛选待处理私信
    假设 在待处理私信列表
    当 在顶部账号选择器选择指定账号
    那么 列表仅显示该账号下的私信
    而且 表格内账号下拉框显示当前选中账号

  场景：待处理私信无数据显示空状态
    假设 无待处理私信
    那么 显示空状态插画
    而且 显示"暂无待处理私信"文案

  场景：重要私信标记显示
    假设 在待处理私信列表
    当 显示重要私信
    那么 显示⭐标记

  场景：AI建议回复
    假设 在待处理私信列表
    当 显示某私信的AI建议回复
    那么 展示AI根据规则生成的建议回复内容

  场景：自动回复私信
    假设 私信符合自动回复条件
    当 点击「自动回复」
    那么 直接使用AI建议回复

  场景：手动回复私信
    假设 在待处理私信列表
    当 点击某私信的「手动回复」
    那么 弹出手动回复Modal弹窗
    而且 显示原私信内容
    而且 输入框预填AI建议回复
    当 修改回复内容并确认
    那么 通过平台集成方式发送回复

  场景：私信记录列表展示
    假设 私信记录列表有数据
    那么 显示时间、发送者、私信内容、状态、操作列
    而且 传统分页，每页10条

  场景：私信记录按日期筛选
    假设 在私信记录列表
    当 选择日期筛选条件
    那么 列表显示筛选后的结果

  场景：查看私信详情
    假设 在私信记录列表
    当 点击某记录的「查看」
    那么 弹出详情弹窗
    而且 显示完整私信内容

  场景：平台不支持系统内操作提示
    假设 平台不支持系统内操作
    当 用户点击手动回复
    那么 弹窗提示用户前往官方App完成

  场景：Token失效跳转登录页
    假设 用户已登录
    当 收到Token失效响应（401）
    那么 显示"登录已过期，请重新登录"
    而且 跳转到登录页

  场景：网络错误显示重试
    假设 页面加载失败
    当 网络连接失败
    那么 显示错误提示
    而且 提供重试按钮

  场景：点击返回按钮
    假设 用户在私信回复页面
    当 点击「返回」按钮
    那么 返回AI助手主页
    而且 路由变为 /ai-assistant
