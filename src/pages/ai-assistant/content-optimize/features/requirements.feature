功能：AI内容优化与重发布

  场景：进入内容优化页
    假设 用户已登录
    当 进入内容优化页面
    那么 显示阈值设置区域
    而且 显示低数据内容列表

  场景：页面初始化显示骨架屏
    假设 页面正在加载
    当 数据加载中
    那么 显示骨架屏

  场景：设置播放量阈值
    当 输入播放量阈值
    而且 点击「重新筛选」
    那么 列表刷新显示符合条件内容

  场景：播放量阈值范围校验
    假设 播放量阈值范围为100-10000
    当 输入超出范围的阈值
    那么 显示格式错误提示

  场景：设置点赞率阈值
    当 输入点赞率阈值
    而且 点击「重新筛选」
    那么 列表刷新显示符合条件内容

  场景：点赞率阈值范围校验
    假设 点赞率阈值范围为0.5%-10%
    当 输入超出范围的阈值
    那么 显示格式错误提示

  场景：低数据内容列表展示
    假设 低数据内容列表有数据
    那么 显示标题、账号、平台、播放量、点赞率、操作列
    而且 播放量为0时点赞率显示0.00%

  场景：无低数据内容时显示空状态
    假设 无低数据内容
    当 进入内容优化页面
    那么 显示空状态插画
    而且 显示"暂无低数据内容"文案

  场景：点击AI优化按钮
    假设 低数据内容列表有数据
    当 点击某内容的「AI优化」按钮
    那么 AI优化按钮显示Loading状态
    而且 AI优化建议区域显示骨架屏

  场景：AI优化完成显示建议
    假设 AI优化请求完成
    那么 显示建议标题（可编辑）
    而且 显示建议标签（可编辑输入框）
    而且 显示「应用」按钮

  场景：应用AI建议
    假设 AI优化建议已生成
    当 点击「应用」按钮
    那么 更新标题和标签
    而且 显示重发布配置区域

  场景：恢复原标题
    假设 已修改建议标题
    当 点击「恢复原标题」
    那么 标题恢复为原始内容

  场景：敏感词拦截
    假设 AI生成内容包含敏感词
    那么 卡片显示红框
    而且 显示"生成内容包含违规词"提示
    而且 「应用」按钮置灰

  场景：AI生成超时
    假设 AI生成请求超时
    那么 显示"生成超时，点击重试"
    而且 提供重试按钮

  场景：重发布配置选择目标账号
    假设 已应用AI建议
    当 选择目标账号
    那么 显示目标账号列表
    而且 默认勾选原账号

  场景：发布方式仅支持立即发布
    假设 用户进行重发布配置
    那么 发布方式仅显示「立即发布」选项
    而且 不显示定时发布选项

  场景：跨账号禁止删除原内容
    假设 已选择目标账号
    当 目标账号不包含原账号
    那么 「删除原内容」复选框置灰
    而且 显示提示文字"仅重发至原账号时可勾选"
    而且 勾选后执行"先发布后删除"策略

  场景：确认重发布
    假设 已选择目标账号
    当 点击「确认重发布」
    那么 调用重发布接口
    而且 按钮显示Loading状态

  场景：跨平台标签限制提示
    假设 同时选择抖音和小红书
    那么 标签上限取最小值5个
    而且 显示提示文字

  场景：标签超限提示
    假设 生成的建议标签超过平台限制
    当 点击「应用」
    那么 Toast提示"标签超限，请手动删减"
    而且 不自动截断标签

  场景：发布成功
    假设 重发布请求成功
    那么 显示成功提示
    而且 更新列表状态

  场景：发布失败网络错误重试
    假设 重发布失败（网络/5xx/429错误）
    当 系统自动重试
    那么 按1min/5min/30min间隔重试
    而且 全失败后标记"发布失败"

  场景：发布失败401/403不重试
    假设 重发布失败（401/403/内容违规）
    那么 直接标记"发布失败"
    而且 不进行重试

  场景：点击返回按钮
    假设 用户在内容优化页面
    当 点击「返回」按钮
    那么 返回AI助手主页
    而且 路由变为 /ai-assistant

  场景：点击标签库按钮
    假设 用户在内容优化页面
    当 点击「标签库」按钮
    那么 跳转到标签库页面
    而且 路由变为 /ai-assistant/tags

  场景：离开页面时有未保存更改
    假设 已生成AI优化建议但未应用
    当 尝试刷新页面、关闭标签页或离开页面
    那么 弹出确认对话框提示"您有未保存的更改，离开后所有更改将丢失。确认离开吗？"
    而且 显示「取消」和「确认离开」两个按钮

  场景：确认离开后不保存更改
    假设 存在未保存的AI优化建议
    当 用户点击确认对话框的「确认离开」
    那么 离开页面，不保存更改

  场景：取消离开保留更改
    假设 存在未保存的AI优化建议
    当 用户点击确认对话框的「取消」
    那么 留在当前页面，保留未保存的更改
