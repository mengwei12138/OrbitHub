功能：AI内容优化与重发布

  场景：页面初始加载默认阈值
    假设 用户已登录系统，已绑定平台账号
    当 访问AI内容优化与重发布页面
    那么 播放量输入框显示默认值「500」
    而且 点赞率输入框显示默认值「2」
    而且 单位标签分别为「次」和「%」

  场景：默认阈值标签显示
    假设 用户已登录系统，已绑定平台账号
    当 访问AI内容优化与重发布页面
    那么 播放量旁边标签为「播放量低于：」
    而且 点赞率旁边标签为「点赞率低于：」

  场景：自定义播放量阈值-有效值100
    假设 用户已登录系统
    当 清空播放量输入框并输入「100」
    而且 点击「重新筛选」按钮
    那么 输入框接受数值100，列表刷新显示播放量小于100的内容

  场景：自定义播放量阈值-有效值500
    假设 用户已登录系统
    当 清空播放量输入框并输入「500」
    而且 点击「重新筛选」按钮
    那么 输入框接受数值500，列表刷新

  场景：自定义播放量阈值-有效值10000
    假设 用户已登录系统
    当 清空播放量输入框并输入「10000」
    而且 点击「重新筛选」按钮
    那么 输入框接受数值10000，列表正常显示内容

  场景：自定义播放量阈值-超出范围99
    假设 用户已登录系统
    当 清空播放量输入框并输入「99」
    而且 点击「重新筛选」按钮
    那么 输入框下方显示红色错误提示「输入值超出有效范围（100-10000）」，列表不刷新

  场景：自定义播放量阈值-超出范围10001
    假设 用户已登录系统
    当 清空播放量输入框并输入「10001」
    而且 点击「重新筛选」按钮
    那么 输入框下方显示红色错误提示「输入值超出有效范围（100-10000）」，列表不刷新

  场景：播放量阈值边界值-100
    假设 用户已登录系统
    当 清空播放量输入框并输入「100」
    而且 点击「重新筛选」
    那么 系统接受输入100，列表正常刷新

  场景：播放量阈值边界值-10000
    假设 用户已登录系统
    当 清空播放量输入框并输入「10000」
    而且 点击「重新筛选」
    那么 系统接受输入10000，列表正常刷新

  场景：自定义点赞率阈值-有效值0.5
    假设 用户已登录系统
    当 清空点赞率输入框并输入「0.5」
    而且 点击「重新筛选」按钮
    那么 输入框接受数值0.5，列表刷新显示点赞率小于0.5%的内容

  场景：自定义点赞率阈值-有效值2
    假设 用户已登录系统
    当 清空点赞率输入框并输入「2」
    而且 点击「重新筛选」按钮
    那么 输入框接受数值2，列表刷新

  场景：自定义点赞率阈值-有效值10
    假设 用户已登录系统
    当 清空点赞率输入框并输入「10」
    而且 点击「重新筛选」按钮
    那么 输入框接受数值10，列表正常显示内容

  场景：自定义点赞率阈值-超出范围0.4
    假设 用户已登录系统
    当 清空点赞率输入框并输入「0.4」
    而且 点击「重新筛选」按钮
    那么 输入框下方显示红色错误提示「输入值超出有效范围（0.5%-10%）」，列表不刷新

  场景：自定义点赞率阈值-超出范围11
    假设 用户已登录系统
    当 清空点赞率输入框并输入「11」
    而且 点击「重新筛选」按钮
    那么 输入框下方显示红色错误提示「输入值超出有效范围（0.5%-10%）」，列表不刷新

  场景：点赞率阈值边界值-0.5%
    假设 用户已登录系统
    当 清空点赞率输入框并输入「0.5」
    而且 点击「重新筛选」
    那么 系统接受输入0.5，列表正常刷新

  场景：点赞率阈值边界值-10%
    假设 用户已登录系统
    当 清空点赞率输入框并输入「10」
    而且 点击「重新筛选」
    那么 系统接受输入10，列表正常刷新

  场景：阈值设置-组合筛选
    假设 用户已登录系统
    当 设置播放量为「500」，点赞率为「2」
    而且 点击「重新筛选」
    那么 列表刷新，显示同时满足播放量小于500且点赞率小于2%的内容

  场景：组合筛选-两者都设最高
    假设 用户已登录系统
    当 设置播放量为「10000」，点赞率为「10」
    而且 点击「重新筛选」
    那么 列表刷新，由于阈值极高可能显示空状态

  场景：组合筛选-两者都设最低
    假设 用户已登录系统
    当 设置播放量为「100」，点赞率为「0.5」
    而且 点击「重新筛选」
    那么 列表刷新，由于阈值极低可能显示所有内容或大量内容

  场景：列表默认分页展示
    假设 存在低数据内容
    当 访问AI内容优化与重发布页面
    那么 列表底部显示「共 N 条」，每页默认10条

  场景：列表表头正确
    假设 存在低数据内容
    当 访问AI内容优化与重发布页面
    那么 表头依次为：标题/文案、账号、平台、播放量、点赞率、操作

  场景：列表内容行正确
    假设 存在低数据内容
    当 访问AI内容优化与重发布页面
    那么 每行显示内容标题、账号名称、平台图标、播放量数字、点赞率百分比、操作按钮「AI优化」

  场景：列表分页-每页条数切换
    假设 存在低数据内容大于10条
    当 选择每页条数为「20条」
    那么 列表刷新，每页显示20条内容

  场景：列表分页-下一页
    假设 存在低数据内容大于10条
    当 点击「下一页」按钮
    那么 列表刷新跳转至第2页，页码高亮显示当前页

  场景：列表分页-上一页
    假设 当前在第2页
    当 点击「上一页」按钮
    那么 返回第1页，列表数据更新

  场景：列表分页-首页
    假设 当前在第2页或更后
    当 点击首页码「1」
    那么 返回第1页

  场景：列表分页-末页
    假设 存在多页数据
    当 点击最后一个页码
    那么 跳转至最后一页

  场景：播放量为0展示
    假设 存在播放量为0的内容
    当 在列表中找到播放量为0的内容
    那么 点赞率显示「0.00%」，页面不报错

  场景：播放量为0点赞率不为报错
    假设 存在播放量为0的内容
    当 观察页面整体
    那么 页面正常加载，无JavaScript错误或白屏

  场景：无低数据内容空状态
    假设 所有内容数据均正常
    当 设置播放量阈值为「999999」并点击「重新筛选」
    那么 列表区域显示空状态插画，下方文字显示「暂无低数据内容」

  场景：空状态无数据时操作按钮
    假设 无低数据内容
    当 观察页面
    那么 无「AI优化」按钮显示，页面保持正常布局

  场景：列表无批量选择框
    假设 存在低数据内容
    当 观察列表左侧
    那么 列表每行左侧无批量选择框，仅显示单条内容的操作按钮

  场景：账号筛选-单账号
    假设 存在多个账号的低数据内容
    当 选择某个账号进行筛选
    那么 仅显示该账号的低数据内容

  场景：账号筛选-全选
    假设 存在多个账号
    当 点击「全选」按钮
    那么 显示所有账号的低数据内容

  场景：平台筛选-抖音
    假设 存在抖音和小红书内容
    当 点击「抖音」筛选按钮
    那么 仅显示抖音平台的低数据内容

  场景：平台筛选-小红书
    假设 存在抖音和小红书内容
    当 点击「小红书」筛选按钮
    那么 仅显示小红书平台的低数据内容

  场景：平台筛选-重置
    假设 已选择平台筛选
    当 选择「抖音」后点击「重置」
    那么 恢复显示所有平台内容

  场景：点击AI优化按钮触发加载
    假设 存在低数据内容，AI服务正常
    当 点击某条内容的「AI优化」按钮
    那么 按钮立即变为Loading状态，不可重复点击

  场景：AI优化建议卡片骨架屏
    假设 点击AI优化后，服务正在生成
    当 点击「AI优化」按钮后立即观察
    那么 建议区域显示骨架屏，非空白

  场景：AI优化成功-标题建议
    假设 AI服务正常响应
    当 点击「AI优化」按钮并等待AI生成完成
    那么 显示优化后的标题文本，标题右侧显示「恢复原标题」按钮

  场景：AI优化成功-标签建议
    假设 AI服务正常响应
    当 点击「AI优化」按钮并等待AI生成完成
    那么 显示3-5个建议标签，标签以#开头，为可编辑输入框样式

  场景：AI优化-抖音平台标签上限提示
    假设 待优化内容属于抖音平台
    当 点击「AI优化」按钮并等待AI生成完成
    那么 建议标签区域显示提示「抖音最多5个，可点击删除/添加」

  场景：AI优化-小红书平台标签上限提示
    假设 待优化内容属于小红书平台
    当 点击「AI优化」按钮并等待AI生成完成
    那么 建议标签区域显示提示「小红书最多10个，可点击删除/添加」

  场景：AI优化成功-置信度展示
    假设 AI服务正常响应
    当 点击「AI优化」按钮并等待AI生成完成
    那么 显示置信度数值和等级（高/中/低）

  场景：AI优化成功-置信度大于等于70%正常流程
    假设 AI置信度大于等于70%
    当 触发AI优化
    那么 置信度大于等于70%时正常生成建议，状态显示正常

  场景：AI优化-置信度小于70%提示
    假设 AI置信度小于70%
    当 触发AI优化
    那么 显示置信度小于70%提示，建议进入人工处理流程

  场景：AI优化卡片最小高度
    假设 AI优化完成
    当 观察AI优化建议卡片
    那么 卡片设置最小高度280px，布局无明显抖动

  场景：建议标签为可编辑输入框
    假设 AI优化完成
    当 点击某个标签文字
    那么 标签变为可编辑状态，光标定位到输入框

  场景：建议标签-增删标签
    假设 AI优化完成，标签处于可编辑状态
    当 在标签区域末尾点击并输入新标签文字
    而且 按回车确认
    那么 新标签被添加到标签列表中

  场景：建议标签-删除标签
    假设 AI优化完成，标签处于可编辑状态
    当 点击某标签的删除图标或选中后按Delete
    那么 标签从列表中移除

  场景：恢复原标题
    假设 已生成AI建议
    当 点击「恢复原标题」按钮
    那么 标题恢复为原始内容

  场景：恢复原标题后应用按钮状态
    假设 已点击恢复原标题
    当 观察「应用」按钮状态
    而且 「应用」按钮恢复为可点击状态

  场景：一键应用AI建议
    假设 已生成AI建议
    当 点击「应用」按钮
    那么 AI建议的标题和标签被应用到当前内容，重发布设置区域可用

  场景：应用后标签超限提示
    假设 AI生成6个以上标签且目标是抖音账号
    当 点击「应用」
    那么 显示Toast提示「抖音最多5个标签，请手动删减」

  场景：应用后标签超限-不自动截断
    假设 AI生成6个标签，目标为抖音账号
    当 点击「应用」
    那么 标签数量不自动截断为5个，需用户手动删除



  场景：AI生成含敏感词-红框提示
    假设 AI服务返回的内容包含敏感词
    当 等待AI返回结果
    那么 建议卡片显示红色边框，提示「生成内容包含违规词，已屏蔽」

  场景：敏感词-恢复原标题后按钮恢复
    假设 AI生成含敏感词
    当 点击「恢复原标题」
    那么 「应用」按钮恢复可点击状态

  场景：AI服务异常-卡片内重试
    假设 AI服务不可用
    当 等待服务响应失败
    那么 卡片显示「生成失败，点击重试」文字和重试按钮

  场景：AI生成超时
    假设 AI服务响应超时
    当 等待超过预设时间
    那么 卡片显示「生成超时，点击重试」

  场景：AI优化按钮防抖
    假设 无
    当 快速连续点击同一内容的「AI优化」按钮2-3次
    那么 按钮在第一次点击后禁用，防止重复提交

  场景：取消恢复AI原始建议
    假设 已生成AI建议
    当 点击「取消」按钮
    那么 标题和标签恢复为AI原始建议内容，列表可选择其他内容进行处理

  场景：重发布设置-默认勾选原账号
    假设 已应用AI优化的内容
    当 观察重发布设置区域
    那么 默认勾选该内容的原账号

  场景：重发布设置-账号选择区域
    假设 已应用AI优化
    当 观察目标账号选择区域
    那么 显示账号列表，包含原账号和其他账号

  场景：重发布-增选其他账号
    假设 已应用AI优化
    当 勾选一个非原账号的复选框
    那么 可增选其他账号，选中后高亮显示

  场景：重发布-单平台单账号限制
    假设 已应用AI优化
    当 先选择抖音账号A后尝试选择抖音账号B
    那么 只能选择一个抖音账号，选择第二个时第一个自动取消选中

  场景：重发布-跨平台独立判断
    假设 已应用AI优化
    当 选择抖音账号A后再选择抖音账号B，然后选择小红书账号C
    那么 抖音平台内单选限制生效，小红书可独立选择

  场景：重发布-跨平台选择
    假设 已应用AI优化
    当 选择一个抖音账号后再选择一个小红书账号
    那么 可同时选择不同平台的账号

  场景：重发布-仅立即发布
    假设 已应用AI优化
    当 观察发布方式选项
    那么 仅显示「立即发布」选项，无定时发布选择

  场景：重发布-跨账号删除限制
    假设 选择了非原账号为目标账号
    当 观察「删除原内容」复选框
    那么 「删除原内容」复选框置灰，右侧显示常驻提示「仅重发至原账号时可删除原内容」

  场景：重发布-原账号可勾选删除
    假设 原账号在目标账号中
    当 观察「删除原内容」复选框
    那么 复选框可正常勾选，无置灰限制

  场景：重发布-取消勾选原账号后删除选项置灰
    假设 原账号已勾选
    当 取消勾选原账号
    那么 「删除原内容」复选框立即置灰

  场景：重发布-确认发布按钮Loading
    假设 已配置重发布设置
    当 点击「确认重发布」按钮
    那么 按钮显示Loading状态，同时禁用不可重复点击

  场景：重发布-取消发布
    假设 已配置重发布设置
    当 点击「取消」按钮
    那么 重发布区域关闭，配置信息不保存

  场景：发布成功-抖音标签超限提示
    假设 发布至抖音账号，标签大于5个
    当 配置重发布设置并点击确认发布
    那么 Toast提示「抖音最多5个标签」

  场景：发布成功-小红书标签超限提示
    假设 发布至小红书账号，标签大于10个
    当 配置重发布设置并点击确认发布
    那么 Toast提示「小红书最多10个标签」

  场景：跨平台发布-取最小标签上限
    假设 同时选择抖音和小红书账号
    当 设置6个标签并点击确认发布
    那么 Toast提示标签超限

  场景：重发布-发布成功状态
    假设 发布成功
    当 点击确认发布并等待发布完成
    那么 显示成功Toast提示，重发布区域显示成功状态

  场景：发布失败-401/403不重试
    假设 Token失效或无权限
    当 触发重发布
    那么 直接标记发布失败，不进行重试，显示失败原因

  场景：发布失败-网络/5xx重试
    假设 网络异常或平台服务异常
    当 触发重发布
    那么 自动重试3次，全失败后标记失败

  场景：发布部分成功-删除原内容失败
    假设 发布成功但删除原内容失败
    当 触发重发布并勾选删除原内容
    那么 提示「新内容已发布，原内容删除失败，请手动处理」

  场景：发布失败-内容违规不重试
    假设 内容违规
    当 触发重发布
    那么 直接标记失败，不重试，提示内容违规原因

  场景：单标签30字符限制-31字符
    假设 新建或编辑标签
    当 在标签输入框输入31个字符
    那么 显示错误提示「单标签最多30字符」

  场景：单标签30字符限制-30字符
    假设 新建或编辑标签
    当 在标签输入框输入30个字符
    那么 系统接受30字符标签，正常保存

  场景：页面初始化骨架屏
    假设 网络正常
    当 首次访问AI内容优化与重发布页
    那么 展示整体骨架屏，包含标题区、卡片区、列表区等主要区块占位符

  场景：列表刷新局部Loading
    假设 列表加载中
    当 触发列表刷新
    那么 表格区域显示局部Loading动画，筛选条件区域不受影响

  场景：按钮提交Loading
    假设 按钮点击提交
    当 点击确认按钮
    那么 按钮进入Loading状态，同时禁用防止重复提交

  场景：无数据空状态
    假设 不存在数据
    当 访问无数据的列表页
    那么 展示插画和「暂无数据」文字，不展示空白表格

  场景：搜索无结果空状态
    假设 搜索条件无匹配
    当 输入不存在的搜索条件并点击搜索
    那么 展示插画和「未找到相关内容，试试调整筛选条件」

  场景：1440px以上完整展示
    假设 屏幕宽度大于等于1440px
    当 访问内容优化页
    那么 完整展示所有表格列

  场景：1024-1439px隐藏平台列
    假设 屏幕宽度在1024-1439px之间
    当 访问内容优化页
    那么 低数据内容列表自动隐藏「平台」列，鼠标悬停账号时Tooltip展示平台信息

  场景：1024px以下横向滚动
    假设 屏幕宽度小于1024px
    当 访问内容优化页
    那么 表格支持横向滚动，内容无遮挡

  场景：AI生成中断-切换页面
    假设 AI优化进行中
    当 切换到其他页面后返回原页面
    那么 任务继续在后台执行，返回页面时主动轮询一次结果状态

  场景：AI生成中断-前端不做Keep-alive
    假设 AI优化进行中
    当 切换到其他页面
    那么 原页面组件已销毁，不保留AI生成状态，返回时重新获取

  场景：筛选条件保留
    假设 已设置筛选条件
    当 切换到其他Tab后返回内容优化页
    那么 筛选条件保留，无需重新设置

  场景：Toast成功提示
    假设 操作成功
    当 触发成功操作
    那么 显示成功提示，2-3秒后自动消失

  场景：Toast失败提示
    假设 操作失败
    当 触发失败操作
    那么 显示失败提示，2-3秒后自动消失

  场景：Toast警告提示
    假设 操作异常
    当 触发异常操作
    那么 显示警告提示，2-3秒后自动消失

  场景：Toast点击关闭
    假设 Toast正在显示
    当 点击Toast关闭按钮
    那么 Toast立即关闭

  场景：确认弹窗-确认操作
    假设 弹窗已打开
    当 点击确认按钮
    那么 执行确认操作，弹窗关闭

  场景：确认弹窗-取消操作
    假设 弹窗已打开
    当 点击取消按钮
    那么 取消操作，弹窗关闭

  场景：确认弹窗-点击遮罩关闭
    假设 弹窗已打开
    当 点击弹窗外部遮罩区域
    那么 弹窗关闭，等同于取消操作

  场景：确认弹窗-ESC关闭
    假设 弹窗已打开
    当 按下ESC键
    那么 弹窗关闭，等同于取消操作

  场景：网络断开-列表重试
    假设 网络断开
    当 访问列表页
    那么 列表区域显示加载失败提示和重试按钮

  场景：接口超时-列表重试
    假设 接口响应超时
    当 访问列表页
    那么 列表区域显示加载超时提示和重试按钮

  场景：Token失效-跳转登录
    假设 Token已过期
    当 触发需要Token的接口
    那么 跳转登录页，提示登录已过期

  场景：播放量为0点赞率处理
    假设 存在播放量为0的内容
    当 找到播放量为0的内容
    那么 点赞率显示「0.00%」，页面不报错

  场景：跨账号删除-前端拦截
    假设 选择了非原账号
    当 配置重发布目标为非原账号并尝试勾选「删除原内容」
    那么 复选框置灰不可勾选，右侧显示常驻提示

  场景：发布失败-401/403立即停止
    假设 Token失效或无权限
    当 触发重发布
    那么 直接标记失败，不重试

  场景：发布失败-站内通知
    假设 发布全部失败
    当 触发重发布并模拟全部失败
    那么 收到站内通知说明失败原因

  场景：发布失败-内容违规不重试
    假设 内容违规
    当 触发重发布
    那么 直接标记失败，不重试，提示内容违规原因

  场景：离开页面-有未保存更改时弹窗
    假设 已生成AI优化建议但未应用
    当 尝试刷新页面、关闭标签页或离开页面
    那么 弹出确认对话框，显示"您有未保存的更改，离开后所有更改将丢失。确认离开吗？"
    而且 显示"取消"和"确认离开"两个按钮

  场景：离开页面-无未保存更改时不弹窗
    假设 不存在未保存的更改
    当 刷新页面或离开页面
    那么 不弹出确认对话框

  场景：勾选删除原内容后刷新页面应弹窗
    假设 已应用AI优化建议且已勾选「删除原内容」
    当 尝试刷新页面、关闭标签页或离开页面
    那么 弹出确认对话框，显示"您有未保存的更改，离开后所有更改将丢失。确认离开吗？"
    而且 显示"取消"和"确认离开"两个按钮

  场景：选择其他账号后刷新页面应弹窗
    假设 已应用AI优化建议且已选择非原账号作为重发布目标
    当 尝试刷新页面、关闭标签页或离开页面
    那么 弹出确认对话框，显示"您有未保存的更改，离开后所有更改将丢失。确认离开吗？"
    而且 显示"取消"和"确认离开"两个按钮

  场景：点击返回按钮-有未保存更改
    假设 已生成AI优化建议但未应用
    当 点击「返回」按钮
    那么 弹出确认对话框

  场景：点击返回按钮-无未保存更改
    假设 不存在未保存的更改
    当 点击「返回」按钮
    那么 直接返回，不弹出确认对话框

  场景：点击标签库按钮-有未保存更改
    假设 已生成AI优化建议但未应用
    当 点击「标签库」按钮
    那么 弹出确认对话框

  场景：点击标签库按钮-无未保存更改
    假设 不存在未保存的更改
    当 点击「标签库」按钮
    那么 直接跳转到标签库页面，不弹出确认对话框

  场景：确认离开后不保存更改
    假设 存在未保存的AI优化建议
    当 用户点击确认对话框的「确认离开」
    那么 离开页面，不保存更改

  场景：取消离开保留更改
    假设 存在未保存的AI优化建议
    当 用户点击确认对话框的「取消」
    那么 留在当前页面，保留未保存的更改

