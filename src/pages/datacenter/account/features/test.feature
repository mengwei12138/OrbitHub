功能：数据中心-账号详情页

  场景：入口1-预警卡片查看详情
    假设 在数据中心主页
    当 点击数据预警区某预警的「查看详情」按钮
    那么 跳转到账号详情页（URL包含账号ID）
    而且 页面显示该账号的详细信息

  场景：入口2-内容列表账号名称
    假设 在数据中心主页
    当 点击内容表现列表的「账号」列名称
    那么 跳转到该内容所属账号的详情页
    而且 页面显示正确账号的信息

  场景：账号已删除-404
    假设 账号已被删除
    当 在浏览器地址栏直接输入该账号详情页URL
    那么 显示404提示页「账号不存在或已被删除」
    而且 页面提供「返回数据中心」按钮

  场景：基本信息完整展示
    假设 账号存在，在账号详情页
    当 观察「基本信息」区域
    那么 展示账号头像（如果有）
    而且 展示账号昵称
    而且 展示平台（Logo+账号名称）
    而且 展示粉丝数
    而且 展示账号状态（在线🟢/离线🔴）
    而且 展示最后同步时间
    而且 最后同步时间格式为"YYYY-MM-DD HH:mm"

  场景：账号状态标识
    假设 在账号详情页
    当 观察账号状态
    那么 在线状态显示🟢绿色标识
    而且 失效状态显示🔴红色标识
    而且 已停止状态显示灰色标识

  场景：今日核心指标展示
    假设 在账号详情页
    当 观察「今日核心指标」区域
    那么 展示4个指标：播放量、点赞量、评论量、转发量
    而且 数据为该账号今日数据总和
    而且 数据与数据中心主页六宫格中该账号数据一致（如筛选该账号）

  场景：近7天播放趋势展示
    假设 在账号详情页
    当 观察「近7天播放趋势」区域
    那么 折线图展示该账号近7天每天的播放量数据
    而且 X轴为日期，Y轴为播放量
    而且 折线图与数据中心主页趋势图数据一致（仅限该账号）

  场景：趋势图数据点悬停
    假设 在账号详情页
    当 鼠标悬停到折线图任意数据点
    那么 显示tooltip，包含日期和具体播放量数值

  场景：近期内容列表展示
    假设 在账号详情页
    当 观察「近期内容列表」区域
    那么 表格列包含：封面、标题、播放量、点赞量、评论量、互动率
    而且 数据为该账号最近发布的内容

  场景：近期内容列表分页
    假设 在账号详情页，内容数量大于10
    当 观察分页器，确认默认每页10条
    而且 点击「下一页」
    而且 等待列表刷新
    那么 页码变为2

  场景：返回保持主页状态
    假设 在数据中心主页设置时间筛选为「近7天」、平台筛选为「抖音」
    而且 在内容表现列表将排序改为「按点赞量」，类型改为「视频」，翻到第2页
    当 点击某账号进入账号详情页
    而且 在账号详情页点击「返回数据中心」
    而且 等待返回主页
    那么 时间筛选仍为「近7天」
    而且 平台筛选仍为「抖音」
    而且 排序仍为「按点赞量」
    而且 类型仍为「视频」
    而且 页码仍为第2页
    而且 滚动位置保持

  场景：网络断开
    假设 网络断开状态
    当 在账号详情页执行任意操作
    那么 显示错误提示「网络连接失败，请检查网络后重试」
    而且 显示重试按钮

  场景：接口超时
    假设 接口响应超时
    当 在账号详情页执行操作
    那么 显示错误提示「数据加载超时，请稍后重试」
    而且 显示重试按钮

  场景：无数据空状态
    假设 某区域无数据
    当 观察各数据区域
    那么 显示对应空状态插画或提示

  场景：Token失效
    假设 Token已过期
    当 访问账号详情页
    那么 跳转登录页
    而且 提示「登录已过期，请重新登录」

  场景：账号详情页Token失效
    假设 Token已过期，在账号详情页
    当 执行任意操作
    那么 跳转登录页
