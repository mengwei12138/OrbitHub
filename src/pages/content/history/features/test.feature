功能：内容发布-历史发布记录

  场景：历史列表-页面布局与列展示
    假设 已有发布记录，用户访问历史发布记录页
    当 观察列表
    那么 列表包含列：封面缩略图、标题/文案、发布账号、平台、发布时间、状态、操作

  场景：历史列表-封面显示
    假设 有成功发布记录
    当 在历史列表页观察封面列
    那么 封面显示平台返回的封面图缩略图

  场景：历史列表-状态标签显示
    假设 有不同状态的发布记录
    当 观察列表中各记录的状态列
    那么 显示「发布成功」「审核中」「发布失败」三种状态标签

  场景：历史列表-时间范围筛选
    假设 历史列表页有多条不同日期的记录
    当 设置开始日期和结束日期
    而且 点击「查询」
    那么 列表只显示该日期范围内的发布记录

  场景：历史列表-平台筛选
    假设 历史列表页有抖音和小红书记录
    当 平台下拉选择「抖音」
    而且 点击「查询」
    那么 只显示抖音平台的发布记录

  场景：历史列表-状态筛选
    假设 历史列表页有多种状态记录
    当 状态下拉选择「发布成功」
    而且 点击「查询」
    那么 只显示「发布成功」状态的记录

  场景：历史列表-多条件组合筛选
    假设 历史列表页有多条记录
    当 设置时间范围
    而且 平台选「小红书」
    而且 状态选「审核中」
    而且 点击「查询」
    那么 只显示同时满足三个条件的记录

  场景：历史列表-「重置」清空筛选条件
    假设 已设置筛选条件
    当 点击「重置」按钮
    那么 所有筛选条件清空恢复默认
    而且 列表刷新显示所有记录

  场景：历史列表-分页翻页
    假设 历史列表有超过1页的记录
    当 点击「下一页」按钮
    那么 分页正确切换
    而且 显示「共N条记录」

  场景：历史列表-点击「查看」进入发布详情
    假设 历史列表页有发布记录
    当 点击某条记录的「查看」按钮
    那么 跳转至该记录的发布详情页

  场景：历史列表-点击「重发」进入重新发布
    假设 历史列表页有发布记录
    当 点击某条记录的「重发」按钮
    那么 跳转至重新发布页
    而且 内容预填充该条记录的原始信息

  场景：历史列表-手动下拉刷新
    假设 用户在历史发布记录页
    当 在列表顶部下拉刷新
    那么 列表重新请求数据
    而且 发布状态同步最新

  场景：历史列表-状态每3小时自动同步
    假设 有「审核中」状态的历史发布记录
    当 等待超过3小时
    而且 刷新历史列表
    那么 发布状态自动更新为「发布成功」
