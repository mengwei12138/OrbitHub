功能：发布详情

  场景：加载详情数据-发布成功状态
    假设 用户进入发布详情页
    当 页面加载时
    那么 显示内容信息区域
    而且 显示发布信息区域
    而且 显示数据表现区域
    而且 发布状态显示「发布成功」

  场景：加载详情数据-审核中状态
    假设 用户进入发布详情页
    当 页面加载时
    而且 发布状态为「审核中」
    那么 内容信息正常展示
    而且 发布状态显示「审核中」
    而且 数据表现区域显示「—」

  场景：加载详情数据-发布失败状态
    假设 用户进入发布详情页
    当 页面加载时
    而且 发布状态为「发布失败」
    那么 显示内容信息
    而且 发布状态显示「发布失败」
    而且 展示失败原因

  场景：数据表现-显示「-」当数据为0
    假设 数据表现为0
    那么 显示「-」而非0
    而且 播放量、点赞量、评论量、转发量、新增粉丝均适用

  场景：手动刷新数据表现
    假设 用户在发布详情页
    当 点击「刷新」按钮
    那么 显示刷新 loading 状态
    而且 调用刷新接口
    而且 更新数据表现
    而且 更新最后同步时间

  场景：数据已停止同步提示
    假设 发布成功超过 30 天
    那么 数据表现区域显示「⚠️ 数据已停止同步」
    而且 提示「发布超过30天，如需最新数据请前往平台查看」

  场景：审核中状态数据表现区域显示「—」
    假设 发布状态为「审核中」
    那么 数据表现区域显示「—」
    而且 提示「内容正在审核中，审核通过后数据将自动同步」

  场景：重新发布入口
    假设 用户在发布详情页
    当 点击「重新发布」按钮
    那么 跳转到重新发布页
    而且 携带该记录 ID 参数

  场景：跳转历史记录页
    假设 用户在发布详情页
    当 点击「返回」按钮
    那么 跳转到历史发布记录页

  场景：查看封面信息
    假设 详情页展示中
    那么 封面以首次发布时平台返回的封面为准
    而且 封面锁定不更新

  场景：互动率计算
    假设 用户查看数据表现
    那么 互动率 = (点赞+评论+转发) / 播放量 × 100%
    而且 正确计算并显示

  场景：加载失败显示错误
    假设 用户进入发布详情页
    当 加载详情数据失败
    那么 显示错误提示
    而且 提供重试选项
