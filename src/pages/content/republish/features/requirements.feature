功能：重新发布

  场景：加载原内容信息
    假设 用户进入重新发布页
    当 页面加载时
    那么 显示原内容标题
    而且 显示原内容文案
    而且 显示原内容标签
    而且 原发布状态显示

  场景：默认勾选原账号
    假设 原发布账号状态为「在线」
    那么 目标账号默认勾选原账号
    而且 原账号可正常勾选

  场景：目标账号-单平台单账号限制
    假设 用户已选择原抖音账号
    当 再选择另一个抖音账号
    那么 原抖音账号自动取消勾选
    而且 新抖音账号变为选中

  场景：目标账号-增选其他账号
    假设 用户已选择原账号
    当 点击增选其他账号
    那么 显示账号选择列表
    而且 用户可选择同平台或不同平台账号

  场景：目标账号-已停止账号不可选
    假设 原账号状态为「已停止」
    那么 原账号显示禁用态
    而且 不可勾选

  场景：目标账号-失效账号不可选
    假设 原账号状态为「失效」
    那么 原账号显示禁用态
    而且 不可勾选

  场景：AI智能优化标题
    假设 用户点击「AI智能优化」按钮
    当 AI 生成标题完成
    那么 自动填充优化后的标题
    而且 显示优化成功提示

  场景：AI智能优化文案
    假设 用户点击「AI智能优化」按钮
    当 AI 生成文案完成
    那么 自动填充优化后的文案

  场景：手动编辑标题
    假设 用户可编辑标题
    当 输入自定义标题
    那么 标题更新为用户输入内容

  场景：手动编辑文案
    假设 用户可编辑文案
    当 输入自定义文案
    那么 文案更新为用户输入内容

  场景：手动编辑标签
    假设 用户可编辑标签
    当 添加或删除话题标签
    那么 标签更新为用户修改结果

  场景：幂等性校验-检测到重复内容
    假设 素材MD5+平台+账号已存在成功发布记录
    当 用户点击确认重发
    那么 显示确认弹窗「检测到该内容已在平台发布过，是否继续发布？」
    而且 用户可选择继续或取消

  场景：幂等性校验-无重复内容通过
    假设 本系统内无相同素材MD5的成功发布记录
    当 用户点击确认重发
    那么 直接提交发布请求
    而且 不弹窗拦截

  场景：确认重发-用户选择继续
    假设 显示幂等性确认弹窗
    当 用户点击「继续发布」
    那么 关闭弹窗
    而且 继续执行发布流程

  场景：确认重发-用户选择取消
    假设 显示幂等性确认弹窗
    当 用户点击「取消」
    那么 关闭弹窗
    而且 不执行发布

  场景：删除原内容-勾选确认
    假设 用户勾选「重新发布后删除内容」
    当 点击确认重发
    那么 弹出二次确认「此操作不可逆，将删除原内容。确认继续？」

  场景：删除原内容-新内容发布成功后执行
    假设 用户勾选删除原内容
    而且 新内容发布成功
    那么 执行删除原内容指令
    而且 原内容被删除

  场景：删除原内容-新内容发布失败时保留
    假设 用户勾选删除原内容
    而且 新内容发布失败
    那么 保留原内容不删除
    而且 向用户发送通知

  场景：删除原内容-用户取消确认
    假设 显示删除二次确认弹窗
    当 用户点击「取消」
    那么 关闭弹窗
    而且 取消删除勾选状态

  场景：重新发布-自动重试3次
    假设 账号发布失败
    当 系统自动重试
    那么 重试间隔 30 秒
    而且 最多重试 3 次

  场景：重新发布-发布成功
    假设 重新发布提交成功
    当 平台返回成功
    那么 显示重新发布成功提示
    而且 跳转历史发布记录页

  场景：重新发布-发布失败
    假设 3 次重试后仍失败
    当 系统判定为发布失败
    那么 显示发布失败提示
    而且 展示失败原因

  场景：封面策略-封面不更新
    假设 重新发布使用原素材
    那么 封面以首次发布时平台返回的为准
    而且 重新发布不更新封面

  场景：防重复提交
    假设 重新发布提交中
    当 500ms 内重复点击
    那么 忽略重复点击
    而且 按钮显示禁用状态

  场景：返回历史记录
    假设 用户在重新发布页
    当 点击取消或返回
    那么 跳转历史发布记录页

  场景：从数据中心进入重新发布页，点击取消返回数据中心
    假设 用户从数据中心点击「去处理」进入重新发布页
    当 点击取消按钮
    那么 跳转数据中心页
